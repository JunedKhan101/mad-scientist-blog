---
const { tagText, tagColor } = Astro.props;
---
<button
	class="m-0 p-[5px] mr-[5px] mb-[5px] border-0 rounded font-bold text-white"
    style={tagColor ? `background-color: ${tagColor}` : null}
>
	{tagText}
</button>
